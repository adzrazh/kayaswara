# Prevent direct access to uploaded files unless downloaded
# Allow file downloads but prevent execution
<FilesMatch "\.(php|phtml|php3|php4|php5|php7|phps)$">
    Order Deny,Allow
    Deny from all
</FilesMatch>

# Prevent directory listing
Options -Indexes
